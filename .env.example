# ===================================
# BrickFlow Environment Configuration
# ===================================
# Copy this file to .env and fill in your values
# NEVER commit .env files to version control

# -----------------------------------
# Server Configuration
# -----------------------------------
# Server port (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# -----------------------------------
# CORS Configuration
# -----------------------------------
# Comma-separated list of allowed origins for CORS
# Example: https://flow.brick.mov,https://app.example.com
ALLOWED_ORIGINS=

# -----------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------
# Primary database connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/brickflow

# Optional: Railway database URL (used automatically in Railway deployments)
# RAILWAY_DATABASE_URL=

# Optional: Fallback database URL (used if primary fails)
# DATABASE_URL_FALLBACK=

# Optional: Public database URL alternatives
# DATABASE_PUBLIC_URL=
# DATABASE_URL_PUBLIC=

# Database SSL configuration (true, false, or auto)
# Default: auto (enabled for remote connections, disabled for localhost)
DATABASE_SSL=auto

# -----------------------------------
# Redis Configuration (Optional)
# -----------------------------------
# Redis connection URL for caching and sessions
# Falls back to in-memory store if not provided
# Format: redis://[username:password@]host:port
REDIS_URL=redis://127.0.0.1:6379

# -----------------------------------
# AI / Google Gemini Configuration
# -----------------------------------
# CRITICAL: Google Gemini API Key for Mason AI assistant
# Get your key from: https://makersuite.google.com/app/apikey
# NEVER expose this key publicly - regenerate immediately if compromised
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------
# Security Notes
# -----------------------------------
# 1. NEVER commit this file with real values
# 2. Use different API keys for development and production
# 3. Rotate API keys regularly
# 4. Add API restrictions in Google Cloud Console:
#    - Restrict to specific APIs (Generative Language API)
#    - Restrict to specific IP addresses or referrers
# 5. Monitor API usage for unexpected activity
